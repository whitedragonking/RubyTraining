<p id="notice"><%= notice %></p>

<%#= link_to 'New Grade', new_grade_path %>

<p id="notice"><%= notice %></p>
<div class="container">

  <p>
  <div class="row">
    <div class="col-lg-12 text-center">
      <h1 class="title">Grades</h1>
    </div>
  </div>
  <label for="">List Grade</label>
  <div class="col-lg-12 form-group" align="right">
    <%= link_to 'New Grade',new_grade_path, :class => "btn btn-success button-function"%>
  </div>
  <table id="table-collapse" class="table table-bordered col-lg-7 table-user">
    <thead>
    <tr>
      <!-- <th>Id</th> -->
      <th>Grade</th>
      <!-- <th>Password hash</th> -->
      <!--<th>Full name</th>-->
      <!--<th>Type</th>-->
      <!--<th>Class</th>-->
      <th></th>
      <!-- <th colspan="3"></th> -->
    </tr>
    </thead>

    <tbody>
    <% @grades.each do |grade| %>
      <tr>
        <!-- <td><#%= grade.id %></td> -->
        <!-- <td><#%= grade.password_digest %></td> -->
        <!--<td><#%= grade.full_name %></td>-->
        <!--<td><#%= grade.role == 0 ? 'Student' : grade.role == 1 ? 'Teacher' : 'Admin'%></td>-->
        <!--<td><#%= grade.class_id %></td>-->
        <td><%= grade.grade_name %></td>
        <td class="text-center">
          <%= link_to 'Show', grade, :class => 'btn btn-sm btn-info button-function button-width'%>
          <%= link_to 'Edit', edit_grade_path(grade), :class => 'btn btn-sm btn-warning button-function button-width' %>
          <%= link_to 'Destroy', grade, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-sm btn-danger button-function button-width' %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
<!-- <%= link_to 'New User', new_user_path %> -->
